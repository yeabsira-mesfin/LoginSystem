{
  "name": "secure-auth-access-control-service",
  "version": "1.0.0",
  "private": true,
  "description": "Application security demo for secure authentication, refresh rotation, revocation, and RBAC",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "supertest": "^7.1.1"
  }
}
